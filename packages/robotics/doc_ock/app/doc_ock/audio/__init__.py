"""Audio/command-source integrations for Doc Ock.

Kept as an optional module so doc_ock core never imports heavy audio deps at
import time. Consumers should import from the submodules explicitly.
"""
